export default (theme) => ({
	editButton: {
		"&:hover": {
			color: theme.palette.primary.main
		}
	},
	deleteButton: {
		"&:hover": {
			color: theme.palette.secondary.main
		}
	},
	content: {
		whiteSpace: "pre-wrap"
	}
});
